const codeHttp = {
	ok: 200,
}

const wordInTag = {
	min: 1,
	max: 2,
}

const startPage = 1;
const stepIncrementPage = 1;
const startCountTodos = 0;

export {
	codeHttp,
	wordInTag,
	startPage,
	stepIncrementPage,
	startCountTodos
}